{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mvn -B -DskipTests clean package"
  },
  "deploy": {
    "startCommand": "java -Dserver.port=$PORT -jar target/community-management-1.0.0.jar"
  }
}
